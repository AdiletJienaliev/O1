## What & why / Что и зачем

<!-- One paragraph: what this changes and the problem it solves.
     Один абзац: что меняется и какую проблему это решает. -->

Closes #

## How it was tested / Как проверялось

<!-- Be specific. "Ran a match as host with 3 bots, captured an outpost, took the forge upgrade."
     Конкретно. «Сыграл матч хостом с 3 ботами, взял аванпост, выбрал кузницу.» -->

- [ ] Project compiles with no console errors / Проект компилируется без ошибок в консоли
- [ ] Play mode reaches a running match / Play mode доходит до идущего матча
- [ ] Checked as host / Проверено на хосте
- [ ] Checked as client / Проверено на клиенте
- [ ] Not applicable / Неприменимо

## Checklist

- [ ] `.meta` files are committed together with their assets / `.meta` закоммичены вместе с ассетами
- [ ] No hard-coded balance values — new numbers live in a config / Баланс не захардкожен, числа лежат в конфиге
- [ ] Synced enums keep their declaration order / Порядок значений в синкаемых enum не менялся
- [ ] Nothing generated (`Library/`, `.agent/`, `*.csproj`) slipped into the diff / В диф не попало генерируемое
- [ ] Comments explain *why*, where the code is not self-evident / Комментарии объясняют «почему» там, где код неочевиден

## Screenshots / Скриншоты

<!-- Required for anything visual. Before / after if it replaces existing behaviour.
     Обязательны для всего визуального. Было / стало, если поведение заменяется. -->
